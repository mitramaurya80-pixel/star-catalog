# star-catalog
A command-line Star &amp; Exoplanet Catalog built in C using a Binary Search Tree (BST). Supports insertion, search, range queries, deletion, level-order traversal, and CSV file persistence.
